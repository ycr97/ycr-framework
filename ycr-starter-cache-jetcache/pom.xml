<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.ycr.framework</groupId>
        <artifactId>ycr-framework</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>ycr-starter-cache-jetcache</artifactId>
    <name>YCR Starter Cache JetCache</name>
    <description>声明式缓存模块 - JetCache 注解（@Cached/@CacheInvalidate/@CacheUpdate）+ Redisson 远程后端</description>

    <dependencies>
        <!-- 依赖 ycr-starter-cache：保证上下文存在 RedissonClient，并与工具类/分布式锁共享同一 Redis 客户端 -->
        <dependency>
            <groupId>com.ycr.framework</groupId>
            <artifactId>ycr-starter-cache</artifactId>
            <version>${revision}</version>
        </dependency>
        <!-- JetCache 自动配置（传递 anno + core，core 自带 Caffeine 本地缓存内核） -->
        <dependency>
            <groupId>com.alicp.jetcache</groupId>
            <artifactId>jetcache-autoconfigure</artifactId>
        </dependency>
        <!-- JetCache Redisson 远程后端：复用上下文已有的 RedissonClient -->
        <dependency>
            <groupId>com.alicp.jetcache</groupId>
            <artifactId>jetcache-redisson</artifactId>
        </dependency>
        <!-- 确保 AOP 自动代理基础设施可用（CacheAdvisor 依赖切面织入） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.3.1</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.1.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.5</version>
            </plugin>
        </plugins>
    </build>
</project>
